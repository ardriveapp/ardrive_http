{
  "name": "js",
  "packageManager": "yarn@3.3.1",
  "devDependencies": {
    "esbuild": "^0.17.3",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "build": "esbuild src/ardrive-http.ts --outfile=dist/ardrive-http.js --minify",
    "watch": "esbuild src/ardrive-http.ts --outfile=dist/ardrive-http.js --watch"
  }
}
